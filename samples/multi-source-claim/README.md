# Sample: multi-source-claim

One claim created by `chris`, reaffirmed by `gemini` and `codex`. Reaffirm events MUST NOT allocate a new `claim_instance_id`; the original instance is the reaffirmation target. Final state: active.
