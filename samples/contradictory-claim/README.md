# Sample: contradictory-claim

Two concurrently-active claims whose `predicate` values contradict (`supports` vs `rejects`) on the same `(subject, object)` pair. Aphelion stores polarity without merging; reconciliation is the consumer's job. Expected verdict: valid.
